pipeline {
    agent any
    triggers {
        GenericTrigger(
            genericVariables: [
                [key: 'ref', value: '$.ref']
            ],

        )
    }

    stages {
        stage('Process Payload') {
            steps {
                script {
                    echo "$ref"
                }
            }
        }
    }
}
