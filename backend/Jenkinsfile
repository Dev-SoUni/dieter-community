pipeline {
    agent any
    tools {
        gradle 'gradle'
    }
    stages {
        stage('Git Clone') {
            steps {
                git branch: 'developer', url: 'https://github.com/Dev-SoUni/dieter-community.git'
            }
        }
        stage('BE-Build') {
            steps {
                print('be-build')
                echo "Branch: ${env.GIT_BRANCH}"
                echo "Commit: ${env.GIT_COMMIT}"
                echo "Repository: ${env.GIT_URL}"
//                 dir("./backend") {
//                     sh "./gradlew clean build"
//                 }
            }
        }
    }
}