pipeline {
    agent any
    triggers {
        GenericTrigger(
            genericVariables: [
                [key: 'payload', value: '$']
            ],

        )
    }


    stages {
        stage('Process Payload') {
            steps {
                script {
                    echo "$payload"
                }
            }
        }
    }
}
